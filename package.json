{
  "name": "typeormproject",
  "version": "1.0.0",
  "description": "Type ORM Project",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc  --build --clean",
    "start": "tsc && node ./build/index.js",
    "watch": "tsc --watch"
  },
  "author": "Imran Rauf",
  "license": "MIT",
  "dependencies": {
    "pg": "^8.11.3",
    "reflect-metadata": "^0.2.1",
    "typeorm": "^0.3.20"
  },
  "devDependencies": {
    "@types/node": "^20.11.24",
    "typescript": "^5.3.3"
  }
}
